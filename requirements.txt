# OfferClaw · 依赖清单
# 安装：pip install -r requirements.txt

# RAG 核心
chromadb>=0.4.0
langchain-text-splitters>=0.2.0
langgraph>=0.2.0

# HTTP API 层
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP 请求
requests>=2.31.0

# 数据模型（FastAPI 依赖）
pydantic>=2.0.0

# 工程自检 / 测试
pytest>=9.0.0
jsonschema>=4.0.0

# JD 抓取 SPA 兜底
playwright>=1.60.0

# 本地 embedding（local provider，可选；不用本地向量可不装）
sentence-transformers>=3.0
modelscope>=1.9   # 国内下载向量模型用，HF 不通时回退
