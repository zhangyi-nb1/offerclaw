cd c:\Users\13513\Desktop\XIAGMU

# 早上开工前：先拉云端最新（拉回 JVS Claw 定时任务夜里写的 daily_log）
git pull

# 改完文件后：三连
git add -A
git commit -m "改了什么的简短描述"
git push

fastapi
cd C:\Users\13513\Desktop\XIAGMU
python -m uvicorn rag_api:app --host 127.0.0.1 --port 8000 --reload
然后浏览器打开 http://127.0.0.1:8000/docs 就是完整的 Swagger UI，可以直接在网页上测试所有接口。